<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tax Glossary</title>
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <style>
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-image : url('/images/glos.jpg');
            background-size: cover;
            margin: 0;
            padding: 0;
        }

        .container {
            width: 50%;
            padding: 20px;
            border: 1px solid #ccc;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            background-color: #fff;
        }

        .former {
            margin-bottom: 20px;
        }

        .form-label {
            font-weight: bold;
            color: #007bff;
        }

        .form-control {
            border: 2px solid #007bff;
            border-radius: 4px;
            font-size: 16px;
        }

        .btn-primary {
            background-color: #007bff;
            border: none;
            border-radius: 4px;
            font-size: 18px;
            transition: background-color 0.3s ease;
        }

        .btn-primary:hover {
            background-color: #0056b3;
        }

        .def {
            font-size: 18px;
            color: #333;
        }
    </style>
</head>
<body>
    <div class="container w-50">
        <form class="former">
            <div class="mb-3">
              <label for="exampleInputEmail1" class="form-label">Enter the word to be searched</label>
              <input type="text" name="word" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp">
            </div>
            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
        <div class="def">

        </div>
    </div>
    <script src="/javascripts/dyanamic.js"></script>
</body>
</html>